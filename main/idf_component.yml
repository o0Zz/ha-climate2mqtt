## IDF Component Manager Manifest File
dependencies:
  espressif/mdns:
    version: "^1.0.3"
    rules:
      - if: "idf_version >=5.0"
  espressif/led_strip:
    version: "^2.5.0"
    rules:
      - if: "idf_version >=5.0"
